# Deployment Docs (Modular)

Use the doc that matches the selected preset. These files are intended to be
copied or composed into client handoff packages as needed.

## Static UI (Default)
- See `docs/STATIC-HOSTING.md` for build and upload steps.

## o2switch API (Backend-Enabled Presets)
- See `docs/O2SWITCH-DEPLOYMENT.md` for Node.js hosting steps.

## Optional SSR/Hybrid
- Only applicable when explicitly enabled in a preset.
- See the o2switch doc for the SSR/hybrid note.
